version: "3.3"
services:
  deno_db:
    container_name: deno_db
    image: mysql:latest
    restart: always
    ports:
      - 3306:3306
    environment:
      MYSQL_DATABASE: deno_db
      MYSQL_USER: deno_user
      MYSQL_PASSWORD: deno_password
      MYSQL_RANDOM_ROOT_PASSWORD: "1"
    volumes:
      - "./temp_db:/var/lib/mysql"
